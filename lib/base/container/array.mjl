struct Array{T, N}
	func::Function
end
getindex(arr::Array, tup...) = arr.func(tup...)

const List{T} where T = Array{T, 1}
const Matrix{T} where T = Array{T, 2}

struct OffsetList{T}
	list::List{T}
	offset::NaturalNumber
end
getindex(arr::OffsetList, i) = arr.list[i+arr.offset]
